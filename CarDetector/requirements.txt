# CarDetector / snapshot workers (aligned with CarDetector README)
# Docker: `torch` / `torchvision` are installed in Dockerfile from PyTorch's CPU wheel index
# so builds do not download ~GB of CUDA/NVIDIA packages.
ultralytics>=8.3.0
opencv-python-headless>=4.10.0
numpy>=1.26.0
psycopg2-binary>=2.9.9
scikit-learn>=1.4.0
joblib>=1.3.0
pandas>=2.2.0
requests>=2.31.0
beautifulsoup4>=4.12.0
sqlalchemy>=2.0.0
